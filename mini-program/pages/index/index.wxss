/* pages/index/index.wxss - 首页样式 */

/* ==================== 顶部欢迎区域 ==================== */
.header-section {
  position: relative;
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  padding: 40rpx 30rpx 30rpx;
  overflow: hidden;
}

.header-bg {
  position: absolute;
  top: -60rpx;
  right: -60rpx;
  width: 240rpx;
  height: 240rpx;
  border-radius: 50%;
  background: rgba(233, 69, 96, 0.1);
}

.header-emoji {
  position: absolute;
  top: -20rpx;
  right: 20rpx;
  font-size: 120rpx;
  opacity: 0.15;
}

.header-content {
  position: relative;
  z-index: 1;
}

.greeting-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
}

.greeting-text {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.date-text {
  font-size: 26rpx;
  color: var(--text-secondary);
}

.header-subtitle text {
  font-size: 26rpx;
  color: var(--text-muted);
}

/* MBTI测试入口 */
.mbti-entry {
  margin-top: 28rpx;
  padding: 24rpx 28rpx;
  background: linear-gradient(135deg, rgba(233,69,96,0.15) 0%, rgba(255,215,0,0.08) 100%);
  border-radius: var(--radius-lg);
  border: 2rpx solid rgba(233,69,96,0.25);
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.mbti-entry:active {
  transform: scale(0.98);
  opacity: 0.9;
}

.mbti-icon { font-size: 48rpx; }

.mbti-text {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.mbti-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--primary-color);
}

.mbti-desc {
  font-size: 22rpx;
  color: var(--text-secondary);
}

.mbti-arrow {
  font-size: 36rpx;
  color: var(--primary-color);
}

/* ==================== 通用Section ==================== */
.section {
  padding: 0 30rpx;
  margin-top: 32rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.section-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.title-icon {
  font-size: 34rpx;
}

.title-text {
  font-size: 32rpx;
  color: var(--text-primary);
}

.more-btn {
  font-size: 24rpx;
  color: var(--text-muted);
  padding: 8rpx 16rpx;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.refresh-btn {
  font-size: 32rpx;
}

.update-time {
  font-size: 20rpx;
  color: var(--text-muted);
}

/* ==================== 实时比分区域 ==================== */
.live-section {
  animation: livePulse 2s ease-in-out infinite;
}

@keyframes livePulse {
  0%, 100% { box-shadow: none; }
  50% { box-shadow: 0 0 30rpx rgba(233,69,96,0.15); }
}

.live-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background-color: var(--primary-color);
  animation: blink 1.2s ease-in-out infinite;
  margin-right: 4rpx;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

.live-label {
  background: var(--primary-color);
  color: #fff;
  font-size: 18rpx;
  font-weight: 700;
  padding: 4rpx 12rpx;
  border-radius: 6rpx;
  letter-spacing: 2rpx;
}

.live-matches-scroll {
  white-space: nowrap;
}

.live-match-list {
  display: inline-flex;
  gap: 20rpx;
  padding-bottom: 10rpx;
}

.live-match-card {
  min-width: 580rpx;
  padding: 28rpx;
  background: linear-gradient(135deg, rgba(233,69,96,0.12) 0%, var(--bg-card) 100%);
  border-radius: var(--radius-lg);
  border: 2rpx solid rgba(233,69,96,0.25);
  white-space: normal;
}

.match-teams {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.team {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  flex: 1;
}

.team-flag { font-size: 48rpx; }

.team-crest {
  width: 56rpx;
  height: 56rpx;
}

.team-name {
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 500;
}

.score-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  padding: 0 20rpx;
}

.score-text {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--secondary-color);
}

.match-minute {
  font-size: 22rpx;
  color: var(--primary-color);
  font-weight: 600;
}

.match-events {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  padding-top: 14rpx;
  border-top: 1rpx solid var(--border-color);
}

.event-item {
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: rgba(255,255,255,0.05);
  color: var(--text-secondary);
}

.event-goal {
  background: rgba(233,69,96,0.15);
  color: var(--primary-color);
}

.event-yellow {
  background: rgba(255,184,0,0.15);
  color: var(--warning-color);
}

/* ==================== 赛程卡片 ==================== */
.matches-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.match-info-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.match-status {
  font-size: 22rpx;
  padding: 4rpx 14rpx;
  border-radius: 8rpx;
  background: rgba(255,255,255,0.05);
  color: var(--text-muted);
}

.status-upcoming {
  background: rgba(0,208,132,0.12);
  color: var(--success-color);
}

.status-live {
  background: rgba(255,59,48,0.15);
  color: #FF3B30;
}

.status-finished {
  background: rgba(108,108,126,0.15);
  color: var(--text-muted);
}

.match-body {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 0;
}

.match-team-info {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  flex: 1;
}

.match-team-flag { font-size: 56rpx; }

.match-team-crest {
  width: 72rpx;
  height: 72rpx;
}

.match-team-name {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--text-primary);
  text-align: center;
}

.match-vs-area {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 30rpx;
}

.vs-text {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-muted);
  letter-spacing: 4rpx;
}

.vs-score {
  font-size: 48rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.score-num {
  color: var(--text-primary);
  transition: all 0.3s;
}

.score-win {
  color: var(--secondary-color);
  transform: scale(1.15);
  display: inline-block;
}

.vs-sep {
  color: var(--text-muted);
  font-size: 32rpx;
}

.match-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--border-color);
}

.match-time {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.match-round {
  font-size: 22rpx;
  color: var(--text-muted);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: rgba(255,255,255,0.04);
}

/* ==================== 黑话卡片 ==================== */
.slang-scroll { white-space: nowrap; }

.slang-list {
  display: inline-flex;
  gap: 20rpx;
  padding-bottom: 10rpx;
}

.slang-card {
  min-width: 480rpx;
  padding: 28rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  white-space: normal;
}

.slang-word {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--secondary-color);
  margin-bottom: 12rpx;
}

.slang-meaning {
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.6;
  margin-bottom: 14rpx;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.slang-tip {
  display: flex;
  align-items: flex-start;
  gap: 8rpx;
  font-size: 22rpx;
  color: var(--text-muted);
  line-height: 1.5;
  background: rgba(255,215,0,0.06);
  padding: 12rpx 16rpx;
  border-radius: var(--radius-md);
}

.tip-label { flex-shrink: 0; }

/* ==================== 球员推荐 ==================== */
.players-scroll { white-space: nowrap; }

.players-list {
  display: inline-flex;
  gap: 20rpx;
  padding-bottom: 10rpx;
}

.player-card {
  min-width: 280rpx;
  padding: 28rpx 24rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  white-space: normal;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.player-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
}

.avatar-placeholder {
  font-size: 48rpx;
  font-weight: 700;
  color: #fff;
}

.player-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 10rpx;
}

.player-meta {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  margin-bottom: 14rpx;
}

.player-team {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.player-position {
  font-size: 22rpx;
  color: var(--text-muted);
}

.player-quote {
  font-size: 22rpx;
  color: var(--text-muted);
  line-height: 1.4;
  font-style: italic;
}

/* ==================== 新闻列表 ==================== */
.news-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.news-item {
  padding: 24rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
}

.news-item:active {
  opacity: 0.85;
  transform: scale(0.99);
}

.news-content {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.news-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-primary);
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.news-desc {
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.news-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 10rpx;
}

.news-date {
  font-size: 22rpx;
  color: var(--text-muted);
}

/* ==================== 世界杯倒计时横幅 ==================== */
.countdown-banner {
  margin: 24rpx 30rpx 0;
  padding: 28rpx 30rpx;
  border-radius: var(--radius-lg);
}

.countdown-pre {
  background: linear-gradient(135deg, rgba(233,69,96,0.15) 0%, rgba(15,52,96,0.6) 50%, rgba(233,69,96,0.08) 100%);
  border: 2rpx solid rgba(233,69,96,0.3);
}

.countdown-live {
  background: linear-gradient(135deg, rgba(233,69,96,0.25), var(--bg-card));
  border: 2rpx solid rgba(233,69,96,0.4);
  animation: livePulse 2s ease-in-out infinite;
}

.countdown-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.countdown-left {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.countdown-emoji { font-size: 56rpx; }

.countdown-info {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.countdown-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #fff;
}

.countdown-subtitle {
  font-size: 24rpx;
  color: rgba(255,255,255,0.7);
}

/* 倒计时数字 */
.countdown-numbers {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.countdown-block {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 64rpx;
}

.countdown-num {
  font-size: 36rpx;
  font-weight: 800;
  color: var(--primary-color);
  line-height: 1;
  text-shadow: 0 2rpx 8rpx rgba(233,69,96,0.3);
}

.countdown-label {
  font-size: 18rpx;
  color: var(--text-muted);
  margin-top: 4rpx;
}

.countdown-sep {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--text-muted);
  margin-bottom: 16rpx;
}

.live-badge {
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding: 12rpx 28rpx;
  background: rgba(233,69,96,0.2);
  border-radius: 40rpx;
  color: var(--primary-color);
  font-size: 26rpx;
  font-weight: 600;
}

.live-dot-sm {
  width: 14rpx;
  height: 14rpx;
  border-radius: 50%;
  background-color: var(--primary-color);
  animation: blink 1s ease-in-out infinite;
}

/* ==================== 焦点对决前瞻 ==================== */
.featured-scroll { white-space: nowrap; }

.featured-list {
  display: inline-flex;
  gap: 20rpx;
  padding-bottom: 10rpx;
}

.featured-card {
  min-width: 480rpx;
  padding: 28rpx;
  background: linear-gradient(145deg, var(--bg-card), rgba(233,69,96,0.06));
  border-radius: var(--radius-lg);
  border: 2rpx solid rgba(233,69,96,0.12);
  white-space: normal;
}

.featured-card:active { transform: scale(0.98); opacity: 0.9; }

.featured-tag {
  display: inline-block;
  padding: 6rpx 18rpx;
  background: linear-gradient(135deg, var(--primary-color), #ff6b8a);
  color: #fff;
  font-size: 20rpx;
  font-weight: 600;
  border-radius: 20rpx;
  margin-bottom: 20rpx;
}

.featured-versus {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.featured-team {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  flex: 1;
}

.featured-crest {
  width: 72rpx;
  height: 72rpx;
}

.featured-name {
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 500;
}

.featured-vs {
  font-size: 28rpx;
  font-weight: 800;
  color: var(--text-muted);
  letter-spacing: 4rpx;
}

/* ==================== 精美空状态 ==================== */
.empty-matches-decorated {
  padding: 0 30rpx;
}

.empty-card {
  padding: 60rpx 30rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  border: 2rpx dashed var(--border-color);
}

.empty-ball {
  font-size: 80rpx;
  animation: ballBounce 2s ease-in-out infinite;
}

@keyframes ballBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-16rpx); }
}

.empty-main-text {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.empty-sub-text {
  font-size: 24rpx;
  color: var(--text-secondary);
  text-align: center;
}

.empty-actions {
  display: flex;
  gap: 24rpx;
  margin-top: 16rpx;
}

.empty-btn {
  padding: 18rpx 32rpx;
  background: linear-gradient(135deg, rgba(233,69,96,0.12), rgba(255,215,0,0.08));
  border: 2rpx solid rgba(233,69,96,0.25);
  border-radius: var(--radius-md);
  font-size: 24rpx;
  color: var(--secondary-color);
}

.empty-btn:active { opacity: 0.7; }

/* ==================== 48强球队速览 ==================== */
.teams-preview-scroll { white-space: nowrap; }

.teams-grid {
  display: inline-flex;
  gap: 20rpx;
  padding-bottom: 10rpx;
}

.team-chip {
  width: 140rpx;
  padding: 24rpx 16rpx 18rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  border: 2rpx solid transparent;
  transition: all 0.2s;
  white-space: normal;
}

.team-chip:active {
  border-color: rgba(233,69,96,0.4);
  transform: scale(0.95);
}

.team-chip-crest {
  width: 68rpx;
  height: 68rpx;
}

.team-chip-name {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--text-primary);
  text-align: center;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 120rpx;
}

.team-chip-group {
  font-size: 20rpx;
  color: var(--text-muted);
  padding: 2rpx 14rpx;
  background: rgba(255,255,255,0.05);
  border-radius: 10rpx;
}
